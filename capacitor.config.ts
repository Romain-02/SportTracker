import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fr.ua.devmob.depenses',
  appName: 'projet-sql-depenses-v19',
  "webDir": "www/FR-fr",
};

export default config;
